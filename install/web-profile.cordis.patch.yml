# 追加到 <DSH_HOME>/profiles/web/cordis.patch.yml 的条目（宿主层 + 浏览器客户端半边）。
#
# 前提：插件包已复制到 <DSH_HOME>/profiles/node_modules/dsh-prompt-self-client/
# （本仓库根目录下 package.json、cordis.patch.yml 与 lib/ 的完整内容），该行通过裸包名解析。
#
# 该行做两件事：
#  1. 宿主层 apply：注册 /prompt-self/profile 与 /prompt-self/config 两条 Web 路由
#     （画像查看与运行时开关读写，引擎本体在 Agent 层预设中）；
#  2. 把 dsh.client 客户端半边注册进浏览器模块表（设置页「Prompt 画像」+ 对话输入坞入口）。

- insert:
    - id: prompt-self-client
      name: 'dsh-prompt-self-client'
